# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.

# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).

# i3 config file (v4)

# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

font pango:Monsterrat 8

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet
exec picom --experimental-backend

set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+Shift+q kill

# screenshot tools
bindsym $mod+t exec "$HOME/dotfiles/config/ocr-screenshotting/text_screenshot.sh"
bindsym $mod+s exec "flameshot gui -c -p $HOME/Pictures/screenshots"
bindsym $mod+Shift+w exec "$HOME/dotfiles/config/i3wm/window_screenshot_flameshot.sh"
bindsym $mod+Shift+a exec "flameshot screen -c -p $HOME/Pictures/screenshots"

# start rofi
bindsym $mod+d exec --no-startup-id "rofi -theme $HOME/dotfiles/config/rofi/applauncher.rasi -show drun -drun-display-format {name}"
bindsym $mod+space exec --no-startup-id "rofi -theme $HOME/dotfiles/config/rofi/applauncher.rasi -show window"
#bindsym $mod+space exec --no-startup-id "rofi -show window"
# rofi/dmenu calculator https://github.com/barbuk/menu-qalc
bindsym $mod+m exec --no-startup-id "=" 
bindsym $mod+i exec --no-startup-id "rofimoji -f alchemical_symbols emojis braille_patterns box_drawing chess_symbols emoticons geometric_shapes gothic greek_extended math mathematical_alphanumeric_symbols mathematical_operators miscellaneous_symbols miscellaneous_mathematical_symbols-a miscellaneous_mathematical_symbols-b miscellaneous_symbols_and_arrows miscellaneous_symbols_and_pictographs miscellaneous_technical modi modifier_tone_letters musical_symbols nerd_font number_forms shorthand_format_controls specials variation_selectors vertical_forms -a copy"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split
bindsym $mod+shift+semicolon split h
bindsym $mod+semicolon  split v


# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+c focus child

set $ws1 "1: Comms"
set $ws2 "2: Browser"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec --no-startup-id "$HOME/dotfiles/config/i3wm/desktop_ctl.sh"

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym $mod+h resize shrink width 10 px or 10 ppt
        bindsym $mod+j resize grow height 10 px or 10 ppt
        bindsym $mod+k resize shrink height 10 px or 10 ppt
        bindsym $mod+l resize grow width 10 px or 10 ppt

        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

exec_always "xkeysnail $HOME/dotfiles/config/xkeysnail/xkeysnail-cfg.py"
exec_always "sleep 4 && setxkbmap -layout us -variant altgr-intl"
exec "volumeicon &"
exec "mate-power-manager &"
exec "nm-applet"

exec "dapper -sue $HOME/.config/autostart"

exec_always --no-startup-id "killall polybar ; polybar"

exec_always --no-startup-id "$HOME/Documents/repos/i3-autodisplay/i3-autodisplay &"
exec_always --no-startup-id "feh --bg-scale ~/Pictures/wallpaper.jpg &"

# Only for main pc, hopefully doesnt break stuff on other devices...
exec_always --no-startup-id 'xinput set-prop "bcm5974" "libinput Tapping Enabled" 1'
exec_always --no-startup-id 'xinput set-prop "bcm5974" "libinput Click Method Enabled" 0, 1'

# notification daemon, discord etc break without it
exec_always --no-startup-id "/usr/bin/dunst &"

focus_follows_mouse no

hide_edge_borders smart

# go back with same key of workspace
workspace_auto_back_and_forth yes

# assign windows to workspaces
assign [class="discord"] $ws1
assign [class="nheko"] $ws1
assign [class="librewolf"] $ws2
assign [class="vivaldi-stable"] $ws2

# color theme
# class					border	backgr.	text	indicator	child_border
client.focused			#504945	#282828	#ebdbb2	#504945		#7c6f64
client.focused_inactive	#504945	#1d2021	#d5c4a1	#664c54		#665c54
client.unfocused		#3c3836	#1d2021	#bdae93	#504945		#504945
client.urgent			#cc241d	#9d0006	#ebdbb2	#fb4934		#7c6f64
client.placeholder		#00ff00	#1d2021	#928374	#00ff00		#3c3836
client.background		#1d2021

for_window [class="^.*"] border pixel 2

gaps top 24
gaps inner 15
